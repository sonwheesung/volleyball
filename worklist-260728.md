# 작업 목록 — 2026-07-28 (사용자 지시 13건)

> 사용자가 채팅으로 준 13건. "미리 문서로 만들어놓고 하나하나" 진행.
> 착수 전 표준 작업 순서(문서→개발→검증)를 각 건에 적용. 코드 근거는 조사 에이전트 2팀이 확인 중.
> **범례**: 🔍 조사/확인 · 🛠 코드 수정 · 🎨 연출/UI · 📣 마케팅/에셋 · 💬 언급만 · ⏸ 오늘 아닐 수 있음

---

## 요약 표

| # | 항목 | 분류 | 문서 근거로 본 초기 판단 |
|---|---|---|---|
| 1 | 스토어 스크린샷마다 설명 캡션(평일밤수식) — kling 제작 | 📣 | 코드 아님. 이미지 위 카피 오버레이 디자인. 에셋 작업 |
| 2 | 광고를 이미지로 올릴 수 있나 (이미지 vs 영상) | 🔍💬 | 정보 답변. Google Ads/UAC는 이미지·영상·텍스트 다 가능 |
| 3 | 경기 전 상대 라인업 열람 + 선수 눌러 상세 | 🛠 | 신규 화면/모달. 조사 중 |
| 4 | 경기화면 선수 이름 **왼쪽에 포지션** 표시 | 🎨 | 마커 렌더 소폭 수정. 조사 중 |
| 5 | 개발자 노트에 데이터 추가 | 💬 | 언급만 (사용자 요청) — 오늘 구현 X |
| 6 | 스파이크 강타 시 **유성 꼬리 + 빨간 궤적** 연출 | 🎨 | 순수 렌더 연출. #122 발열 규칙(useNativeDriver·리렌더0) 준수 필요 |
| 7 | 경기화면 **세트 종료 버튼** (결과=경기 즉시종료만 있음) | 🛠 | "⏭ 결과" 버튼 존재. 세트단위 종료는 신규. 조사 중 |
| 8 | **세터 성향** 추가(속공 선호·안전형 등) | ⏸🛠 | 신규 엔진 기능. 오늘 아닐 수 있음(사용자 명시) |
| 9 | 테크니컬 타임아웃 = 5세트만? 확인 요청 | 🔍 | **사용자 생각과 반대**: TTO는 1~4세트(8·16점), 5세트엔 없음(KOVO 정확). ↓상세 |
| 10 | 핸들링 범실이 **자기에게 토스**하며 남 (토스도 안 하고 범실) | 🛠🔍 | **BOARD_RULES 룰19(2026-06-18)에서 이미 수정했다고 기록된 버그의 재발**. 회귀 조사 필요. ↓상세 |
| 11 | **네트 터치 범실**이 한 번도 없음 — 추가하려는데 공수 큰지 확인 | 🔍🛠 | 현재 네트터치는 `miscErr`로 통합(별도 판정 아님). 별도 유형화=작업. ↓상세 |
| 12 | 경기 종료 후 **그 경기 MVP**도 뽑지 않나? (세트스코어만 뜸) | 🔍🛠 | 조사 중 (경기 단위 MVP 존재 여부) |
| 13 | 경기 **종료 후 홈으로** 이동 (중간이탈은 무관, 끝나면 홈→뉴스 유도) | 🛠 | 네비게이션 수정. 조사 중 |

---

## ✅ 코드 조사 완료 (에이전트 2팀 · 파일:라인 근거)

| # | 확정 사실 | 근거 |
|---|---|---|
| 3 | 상대 라인업 사전 열람 **미구현**. 일정탭 "다음 경기" 카드는 팀 OVR 배지만, 개별 명단 없음 → 바로 보드 진입 | `schedule.tsx:147-278`, 상세화면 `player/[id].tsx`는 존재하나 미연결 |
| 4 | 코트 마커: 원 안=**등번호**(jerseyNo), 아래=**이름만**. 포지션은 **테두리 색만**(글자 없음) | `MatchCourt.tsx:619-660`, POS_COLOR `posTokens.ts:7` |
| 6 | 스파이크 궤적=**틸/민트 점선 17개**(trailDots) + 3색 배구공 회전. **빨강·유성 꼬리 없음**. 전부 순수렌더(useNativeDriver) | `MatchCourt.tsx:596-607·894` |
| 7 | "⏭ 결과" 버튼=경기 전체 즉시종료(endNow: 랠리 끝으로 점프). **세트 종료 버튼 없음**(문자열 자체 부재) | `MatchCourt.tsx:743·413-416` |
| 9 | TTO=**1~4세트만**, 8·16점, 5세트 제외. KOVO 정확 일치. **버그 아님** | `match.ts:58·636` |
| 10 | 엔진엔 "자기 토스" 로직 없음(세터/공격수 분리 선택). miscErr는 확률 롤. 보드 연출 2경로 존재: 417행=passSpot(세터근처) 낙구 / 615행=공격수쪽 낙구. 가드 `_dv_mischandle` PASS(스파이크0) | `rally.ts:500-507`, `courtPath.ts:417·615` |
| 11 | 네트터치=독립판정 없음. `miscErr`(볼핸들링)·`tip`(팁) 범실군에 통합. 현 종결유형: ace/serveErr/recvErr/fault/miscErr/kill/blockout/stuff/atkErr/tip | `rally.ts:111-115·606` |
| 12 | 경기 MVP **구현됨**(`matchMvp` — 승리팀 득점+디그가산). 단 **`matchresult/[id].tsx`에만** 표시. 관전보드 종료 오버레이엔 **세트스코어만** | `data/matchAward.ts:21`, `matchresult/[id].tsx:36-52` vs `match/[id].tsx` |
| 13 | 경기 종료 후 **자동 이동 없음** — 보드에 종료 오버레이 뜬 채 머묾. "나가기"→`router.back()`(일정 복귀). 홈 강제이동 없음 | `match/[id].tsx:152-166·165` |

### 진행 현황 (2026-07-28)
- ✅ **4** 포지션 표시 — `MatchCourt.tsx` nameTag에 POS_COLOR 포지션 약자 prefix
- ✅ **6** 유성 연출 — 강타 스파이크(kind spike·!soft) trailDots 빨강/주황 유성(머리 백열→꼬리 적색, 크기·투명도 페이드). 순수렌더, 골든 불변
- ✅ **7** 세트 종료 버튼 — "⏭ 세트" 신설(현재 세트 마지막 랠리로 점프, 다음 세트 이어보기). 확인 팝업
- ✅ **12+13** 종료 후 흐름 — `match/[id].tsx` finished 나가기 → `matchresult?from=watch`(MVP+박스) → "홈으로(뉴스 보기)"
- ✅ **3** 상대 라인업 — 일정 카드 "상대 라인업 보기" → 예상 선발6+리베로 모달(buildLineup 순수, 시뮬 무실행=무스포일러) → 선수 탭 상세
- 검증: tsc 0 · test tsc 0 · npm test 218 pass · 골든 v17 불변 · auditBoard 0건 · _dv_mischandle pass
- ✅ **10** 자기 토스 재발 — 원인=417행 조기 return(passSpot 즉사)이 룰19 toss낙구(615행) 가림. 수정=417 제거+615 무조건발화. 가드 `_dv_mischandle`에 긍정불변식(종결 전 toss) 추가, **A/B 205/205 FAIL→0**. 골든 불변·auditBoard 0·checkBoardFixes·_dv_cover pass. 문서: BOARD_RULES 룰19b·TEST_METHODOLOGY 사각
- ⏸ **8**(세터 성향)·💬 **5**(개발자노트)·📣 **1·2**(에셋/정보) — 아래 별도

### 실행 계획 (확정)
- **4**: `nameTag`에 포지션 약자 prefix("S 김선수"). 색은 POS_COLOR 유지. 소수정
- **12+13(한 몸)**: 종료 오버레이에 MVP 카드 추가(matchMvp 재사용) + 종료 후 "나가기"는 **홈으로**(`router.replace('/(tabs)')`), 중간이탈은 현행 `router.back()` 유지
- **6**: trailDots를 강타 킬에 한해 **빨강 유성(꼬리 페이드)** 로. 순수렌더·리렌더0(#122)
- **7**: "⏭ 세트" 버튼 신설 — 현재 세트 끝 랠리로만 점프(경기 전체는 기존 "⏭ 결과")
- **3**: 일정 "다음 경기" 카드에 "상대 라인업 보기" → 선발6 모달, 선수 탭→`player/[id].tsx`
- **10**: **에뮬/보드랩 재현 먼저**(추정 금지) → 실제 새는 경로 특정 → 수정 → `_dv_mischandle` 강화(passSpot 자기근처 낙구도 검사)
- **9**: ~~버그 아님 설명~~ → **정정(2026-07-28)**: 검색으로 확인 시도 → FIVB 2021 폐지 확실, KOVO 유지 여부는 1차 출처 확정 불가(namu 403). 사용자 요청으로 **엔진 TTO 비활성**(`TTO_ENABLED=false` 게이트, 부활 가능). 밸런스 중립(8·16점 생리 회복만 유지 → 피로 교체율 0.45 밴드 보존, 통째로 빼면 0.89 폭주였음). ENGINE_VERSION 17→18·골든 재생성. 검증: tsc0·npm218·simKovo 밴드·simStamCurve PASS·_dv_manual_side(TTO_ENABLED 인식)·auditBoard0
- **11**: **보류 확정(2026-07-28 사용자)** — 출시 후 과제. 오늘 X
- **12+13 흐름 확정(2026-07-28 사용자)**: 관전 종료 후 "나가기" → **경기 상세결과(`matchresult/[id]`, MVP+박스)** → 거기서 나가면 **홈**. 경기 중 나가기는 현행(`router.back` 일정 복귀) 유지
- **8**: ⏸ 오늘 아닐 수 있음(세터 성향 엔진 신규)
- **1·2·5**: 에셋/정보/언급

---

## 상세 (문서 근거 확인분)

### 9. 테크니컬 타임아웃 — 확인 완료 (사용자 인식이 반대)
`docs/MATCH_SYSTEM.md §7.4b` (2026-07-07 구현):
- TTO는 **1~4세트**에서 리드팀 점수가 **8·16에 처음 도달**할 때 각 1회(세트당 최대 2회).
- **5세트에는 TTO 없음** (KOVO는 5세트 8점 코트체인지만, 코트 추상화라 시뮬 무영향).
- 즉 "5세트만 TTO"는 반대 — **현재 구현이 KOVO 규칙과 일치**. 코드 실측으로 최종 확인(에이전트).
- → **결론**: 버그 아님. 사용자에게 규칙 설명. (원하면 관전 중 TTO 라벨 노출 강화 등 별도 논의)

### 10. 핸들링 범실 자기 토스 — 재발(회귀) 의심
`docs/BOARD_RULES.md 룰19` (2026-06-18)에 **동일 증상이 이미 수정됨**으로 기록:
> "볼핸들링 범실(더블/캐치) 때 세터가 자기에게 토스한다 — miscErr 연출을 정식 toss WP(공격수 방향) 뒤 휘슬 낙구로. 공이 공격수 쪽으로 뜨다 만다(자기에게 아님)."
- 사용자가 지금 **다시** 이 증상을 봄 → (a)회귀 (b)수정 불완전 (c)다른 코드경로 중 하나.
- → 버그 발견 프로토콜: 재현 → 룰19 코드 현존 확인 → 왜 새는지 → 형제 사냥 → 가드. 조사 중.

### 11. 네트 터치 범실 — 현재 별도 유형 아님
`docs/KOVO_RULES_COMPARISON.md §23`: 볼핸들링(더블·캐치·**네트터치**)이 `rally.ts`에서 **`miscErr`로 통합**.
- 네트터치가 독립 판정/결과종류로 존재하지 않음(miscErr 우산 안). BOARD_RULES 룰50(23)은 블로킹 시 네트터치 **시각 침범** 방지(연출)일 뿐 판정 아님.
- 별도 "네트터치 범실" 유형화 = 엔진 결과종류(PointLog.how) 추가 + 확률 판정 + 보드 연출 + 박스 + KOVO 분포 재튜닝 → **공수 큼**. 사용자 우려 타당.
- → 조사 후 "간이(공격/블로킹 miscErr에 네트터치 라벨만 분화)" vs "정식 유형" 비용 비교 제시.

### 6. 스파이크 유성 연출 — 선례 있음(순수 렌더)
`docs/MATCH_SYSTEM.md 8장 말미`: 서브 궤적 변조(스파이크 커브/플로터 흔들림)가 이미 **순수 연출**(엔진 RNG 미소비·useNativeDriver·리렌더0, #122 발열 회피)로 존재.
- 유성 꼬리+빨간 궤적도 같은 원칙으로 추가 가능(강타=킬 종결에만). 조사 후 위치 특정.

---

## 진행 방식
- 코드 조사 2팀(경기엔진 범실/MVP·보드UI/네비/연출) 결과 수신 후 이 표를 확정.
- 확정 순서(제안): 9(확인·설명) → 12(확인) → 4·13(작은 UI) → 10(회귀수정) → 3·7(신규기능) → 6(연출) → 11(비용판단) → 8(⏸) → 1·2·5(에셋/정보/언급).
