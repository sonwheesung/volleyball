// 태스크 #131 시나리오 C — 랠리 중 전환 수비 대형(defTransition) 전용 파라미터.
// 값마다 근거 주석 의무(정본: docs/COURT_POSITIONING.md §4.9 · POSITIONING_TUNING_LOG.md R1·R2 실측 좌표 출처).
// **통과조건 6**: 기존 검증 좌표(receiveFormation·serveFormation·switchedSpots·fanSlots 상수)는 여기로 이동하지 않는다.
//   여기 있는 건 전부 시나리오 C에서 새로 도입된 값뿐이다.

// ── 전위 블록 레디 라인 (상대 세트 전 네트에 붙어 3인 블록 준비) ──
// 깊이(홈 프랙션, 네트=0.5) — laneRank 0(좌윙)·1(센터)·2(우윙). serveFormation 전위 깊이(frontDepthFrac 0.555~0.605)와
// 같은 결(네트 밴드 ≤1.3m), 센터가 네트 최밀착. NET_SAFE(26px)와 정합: 홈 0.55*H=308이 네트 280 위 28px(코트 H=560 기준).
// 근거: COURT_POSITIONING B-1/시나리오 C(세터 후위 → 전위 3 블록 라인), R2 관찰(블록 레디 국면 부재).
export const BLOCK_READY_Y_HOME = [0.575, 0.55, 0.60];

// 번치 x(홈 프랙션) — 중앙 3분의 1로 모음. serveFormation SV_FRONT_X([0.36,0.5,0.64])와 **같은 결**(courtLayout 주석:
// GMS·USAV 번치 리드 — 넓게 안 펴고 중앙 뭉쳐 상대 세터 읽고 핀으로 릴리즈). 좌우 순서(스페셜리스트 레인) 보존.
// 별도 상수로 둠 — SV_FRONT_X는 serveFormation 검증 좌표라 이동 금지(통과조건 6).
export const BUNCH_X_HOME = [0.36, 0.5, 0.64];

// ── 후위 페리미터 (코트를 넓게 덮는 딥 베이스 — 라인딥/크로스딥/리베로 셰이드) ──
// **중립 컵 채택(통과조건 2 "중립 컵만" 허용)**: pass 국면엔 아직 공격 코스를 모르므로(미래 토스 x 미참조)
//   컵을 공격 x로 흔들지 않는다 — 세그먼트마다 앵커(공 x)로 셰이드하면 pass↔toss 진동이 새로 생겨(왕복 급증) 역효과.
//   toss 구간부터 기존 fanSlots(공격 x)로 수렴하며 "읽고(딥) → 커밋(컵 전진)"이 자연스럽게 나온다.
// 깊이는 fanSlots(윙 0.72·중앙 0.85)에 **근접**시켜(윙 0.76·중앙 0.87) pass↔toss 전환을 임계 이하로 매끄럽게 —
//   R1의 "윙도 딥"(0.805) 지향은 살리되(fanSlots보다 깊게), fanSlots는 스펙상 무변경(통과조건 6)이라 그 프레임보다
//   과하게 깊으면 진동이 커진다. 근거: R1 실측(원정 수비 z1[.204,.194]·z5[.784,.196]·L[.526,.142]) + fanSlots 정합.
export const PERIM_WING_X = 0.21;        // 양 윙 사이드라인 안쪽 프랙션(좌윙=0.21 · 우윙=0.79 — fanSlots 0.22 정합)
export const PERIM_WING_Y_HOME = 0.76;   // 양 윙 깊이(홈 프랙션 — fanSlots 0.72보다 딥, 진동 임계 이하)
export const PERIM_CENTER_Y_HOME = 0.87; // 리베로/중앙 최심(홈 프랙션 — 코트 최후방, R1 L 최심 나드)
export const PERIM_SHADE = 0.0;          // 앵커 셰이드 미사용(중립 컵) — 진동 회피. 값 0으로 봉인(구조 보존).
export const PERIM_WING_SHADE = 0.0;     // 동상

// ── 오프블로커(벽 미참여 전위) 자기 사이드 유지 (룰 48 방향 반전 — 태스크 #131 R3) ──
// 근거(POSITIONING_TUNING_LOG R3, 사용자 board-lab 실측): 구 동작(공격 x쪽 0.35 시프트 → 중앙으로 끌림)은 다른 수비
//   커버 구역과 **중복**이라 무의미하고, **자기 사이드 예각 크로스/라인 스파이크**를 무방비로 열었다. 팁(페인트) 커버는
//   후위 담당 유지(오프블로커 재배정 금지). 새 목표 = 자기 스위칭 레인(전위 존) 근처·네트밴드 하단.
//   실측 목표 예: 홈 우측 레인 OP [0.694, 0.570] — switchedSpots 전위 우측 0.8W서 안쪽 0.10 인셋 → 0.7W, y0.575H.
export const OFFBLOCKER_Y_HOME = 0.575;  // 오프블로커 깊이(홈 프랙션 — 네트밴드 하단, 구 0.66=3m선보다 네트 쪽)
export const OFFBLOCKER_INSET = 0.10;    // 자기 스위칭 레인서 코트 중앙 쪽으로 소폭 안쪽(프랙션) — 라인 여백 확보

// ── 공격 커버 반원 타이트닝 (전면 공격 기준 — 태스크 #131 R4) ──
// 근거(POSITIONING_TUNING_LOG R4, 사용자 board-lab 실측 — 원정 공x0.800): 남주솔[0.914,0.446]·염슬연L[0.676,0.437]·
//   엄연윤[0.729,0.371] → 홈 프랙션 환산. 원리: 블록 리바운드는 히터 바로 뒤 네트 근처에 떨어진다 → 커버 반원을
//   히터 쪽으로 당기고(근접=전위 존 진입) 라인 쪽은 사이드라인까지 벌려 낙하 구역을 감싼다. "근접 2+후방 1" 유지.
//   방향 일반화: 라인=히터 쪽 사이드라인 방향, 안쪽=코트 중앙 방향(attackX가 좌/우 어느 쪽이든 대칭).
export const COVER_NEAR_Y_HOME = 0.56;   // 근접 2슬롯 깊이(홈 프랙션 — 전위 존, 네트 뒤 낙하 구역). 구 0.645. 실측 0.554/0.563.
export const COVER_DEEP_Y_HOME = 0.63;   // 후방 1슬롯 깊이(홈 프랙션 — 3m 라인 부근). 구 0.70. 실측 0.629.
export const COVER_LINE_DX = 0.114;      // 라인 쪽 근접 = attackX + 사이드라인 방향 이만큼(W 분수). 실측 0.914−0.800.
export const COVER_INSIDE_DX = 0.124;    // 안쪽 근접 = attackX − 중앙 방향 이만큼(W 분수). 실측 0.800−0.676.
export const COVER_DEEP_DX = 0.071;      // 후방 = attackX 중앙 쪽 이만큼(정후방 아님, W 분수). 실측 0.800−0.729.

// ── 커버 2인 시 층(근접1+2선1) — 태스크 #131 R5 ──
// R4의 형제 부작용 교정: n=2 분기가 둘 다 근접(y0.56)이라 커버+세터+전위가 네트 밴드에 몰림(전위 6명 — 전술 비현실).
// 커버는 인원 무관 **층(근접+2선)** 유지 — 근접 1(리베로 우선·수비 전문), 2선 1(후위 공격수 우선 = 백어택/파이프 준비 겸).
// 근거: R5 사용자 board-lab 실측(홈 open x0.200): 차니 후위OH 2선[0.104,0.710]·현한정L 근접[0.304,0.572]. 배정은 호출부(courtPath).
export const COVER_SECOND_Y_HOME = 0.71;  // 2선 슬롯 깊이(홈 프랙션 — 3m 뒤, 백어택 발사 라인 겸). 실측 차니 0.710.
export const COVER_SECOND_DX = 0.096;     // 2선 x = attackX + 라인(사이드라인) 방향 이만큼(W 분수). 실측 0.200−0.104.
export const COVER_NEAR1_DX = 0.104;      // n=2 단독 근접 x = attackX + 안쪽(중앙) 방향 이만큼(W 분수). 실측 0.304−0.200.
