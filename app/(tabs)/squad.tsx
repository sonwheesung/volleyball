import { Card, Muted, Screen } from '../../components/Screen';

export default function Squad() {
  return (
    <Screen title="선수단 / 스탯">
      <Card>
        <Muted>선수 카드와 2층 스탯(밑단 세부 → 윗단 종합)을 여기에 표시합니다. (Phase 2)</Muted>
      </Card>
    </Screen>
  );
}
