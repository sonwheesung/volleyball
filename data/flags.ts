// 빌드/노출 플래그 — 개발용 도구를 실전(배포)에서 숨긴다.
// DEV_TOOLS=false 이면: 테스트 경기·보드 위치 검증·영입 무결성 감사 화면/버튼이 전부 비활성.
//   버튼은 렌더 안 하고, 화면에 직접 진입(딥링크)해도 홈으로 리다이렉트한다.
//
// __DEV__ 에 묶는다(RN 전역) — 빌드 종류가 자동 결정. 손으로 만질 일 없음:
//   · 개발(Expo Go·metro dev) → __DEV__=true  → 테스트 도구 보임
//   · 프로덕션 빌드(EAS release) → __DEV__=false → 자동으로 전부 숨김(깜빡 잊을 일 없음)
export const DEV_TOOLS = __DEV__;
