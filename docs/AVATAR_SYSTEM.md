# 선수 아바타 시스템 (AVATAR_SYSTEM)

> 유대감 강화(독립리뷰 2026-07-04, 4에이전트 합의 1순위 공백)의 첫 조치. "전원 회색 아이콘"이라 정 붙일
> 얼굴이 없던 문제를 해결한다. 관전형·데이터 누적 서사 두 기둥과 정합(얼굴이 있어야 기록·활약이 "정든 개인"에 쌓인다).

## 결정 (2026-07-04)

- **1차: 실제 얼굴 스프라이트 시트**(사용자 제작, GPT 이미지) — 크로마키 **초록 배경**으로 **3×3(9명) 시트**를 받아, 초록을 프로그램으로 제거(테두리+디스필)한 **투명 PNG**(`assets/players/facesN.png`)를 만든다. `id 해시 % 얼굴수 → 칸` 결정론 배정, 런타임에 그 칸만 크롭(`View overflow:hidden` + `Image` 오프셋 = 스프라이트). 투명 배경 뒤엔 id별 파스텔.
  - **현황(2026-07-05)**: faces1~11 = **11장 × 9 = 99명 풀**(목표 300 → ~33장). cowork(Claude-in-Chrome)가 대표이미지+프롬프트로 새 채팅마다 생성→`_intake/`(gitignore) 저장→채팅삭제(화풍 드리프트 방지) 반복, `tools/keyFaces.ts`가 고정 파라미터로 크로마키 제거.
  - **재개(2026-07-19)**: faces12~18 추가 = **18장 × 9 = 162명 풀**. 같은 파이프라인(레퍼런스 `_intake/_reference.png` + 새 채팅). **프롬프트 정정(사용자 피드백)**: 헤어 예시에 "숏컷"이 있으면 매 시트마다 거의 동일한 보이시 숏컷이 1명씩 복제됨(레퍼런스 하단좌측 숏컷 앵커) → 예시에서 숏컷 제거 + **"남자처럼 보이는 아주 짧은 숏컷/픽시컷 금지, 어깨 위 단발 이상"** 명시로 해결(6번째 생성분은 폐기·`_intake/20.png`, faces17부터 금지판 적용). faces12~16에는 시트당 1명꼴로 유사 숏컷이 이미 포함 — 교체 여부는 사용자 판단 대기.
  - 초록 채택 경위: ~~검정 배경~~은 검은 머리와 구분 불가라 플러드필 시 머리 손상 → **초록 크로마키**로 교체(피부·머리·유니폼·입술에 없는 색). AI 투명 출력 불가 → 단색 초록으로 받아 우리가 제거.
  - 시트 추가 = `FACE_SHEETS`에 push + count. 시트 초과 id는 아래 절차적 아바타로 폴백(무한 세대교체 안전).
  - **화풍 통일(2026-07-04, 사용자 선택 A)**: **반실사 애니풍**으로 확정. ~~초기 플랫 벡터 시트(구 faces1~4, 100명)~~는 톤이 달라 폐기하고 반실사 시트로 전량 교체. 이후 모든 시트는 이 톤으로만(섞으면 로스터 화풍 불일치). 시트 추가 시 `id%전체수` 재배정 → 풀 확정 전 얼굴 변동은 정상.
- **2차(폴백): 온브랜드 절차적 레이어 아바타** — 시트에 없는 id는 `react-native-svg`로 얼굴 레이어를 직접 그린다(LegendIllustration 스타일 재사용).
  - ~~multiavatar/DiceBear 등 생성 라이브러리~~ 반려: DiceBear는 `<style>` CSS라 react-native-svg에서 검은 실루엣, multiavatar는 렌더는 되나 **장난스러운 톤·성별 섞임**(여자 V리그 부적합). 온브랜드가 톤·성별·유니폼을 통제.
- **여자 V리그 톤** — 여성형 헤어스타일 위주, 유니폼(팀색) + 브이넥.
- **id 시드 결정론·무저장** — 같은 선수 = 항상 같은 얼굴. 100시즌+ 세대교체로 이름이 바뀌어도 id 고정. 저장 없이 재계산(기둥 정합).

## 구조

- `data/playerFace.ts` — `faceCell(id)`: 실제 얼굴 시트 칸 배정(없으면 null=폴백). `FACE_SHEETS`(시트 목록·격자·count). + `faceFeatures(id)`: 파스텔 배경 + 절차적 폴백용 피처(피부5·헤어7·배경6·스타일5·눈3·입3, 저장 X).
- `components/PlayerAvatar.tsx` — `<PlayerAvatar id size />`: `faceCell` 있으면 파스텔 원 + 시트 칸 크롭 렌더, 없으면 SVG 레이어.
- `assets/players/facesN.png` — 초록 제거된 투명 얼굴 시트(3×3=9명). 슬라이싱은 오프라인 전처리(`tools/keyFaces.ts`, sharp `--no-save`), 앱은 require+오프셋만. 파라미터 고정 = 모든 배치 동일 투명도(재현성).
  순서: 배경 → 뒷머리 → 목 → 유니폼(어깨)+브이넥 → 머리+귀 → 앞머리(뱅) → 눈썹·볼터치 → 눈 → 입.
  헤어스타일 0 롱·1 포니테일·2 단발·3 올림(번)·4 숏. 유니폼색은 `jersey`/`trim` prop(기본 틸, 팀색 주입 가능).

## 노출 현황 / 향후

- ✅ **선수 상세 히어로**(`app/player/[id].tsx`) — 회색 아이콘 → 아바타.
- ⏳ 향후: 선수단 목록·드래프트·경기 MVP·헌액 등 다른 표면으로 확대(유대감 후속 로드맵의 얼굴 토대).

## 검증

- `tools/_dv_face.ts` — 결정론(같은 id 동일 피처) + 변형(5스타일·5피부·7헤어·6배경 전부 등장·편중<40%). tsc 0.
- 렌더는 react-native-svg 프리미티브(Path/Circle/Ellipse/Rect)만 사용 — 앱 곳곳(Legend/Champion/Award 일러스트·레이더)에서 이미 검증된 경로.

## 원칙 합치

- 관전형: 보는 경험 1순위 투자처와 정합(얼굴은 몰입 자산). 손 안 감·무푸시 무관.
- 누적 서사: 얼굴이 기록의 "그릇" — 통산·마일스톤·헌액이 정든 개인에 귀속.
- 결정론·무저장: id 파생 재계산. 가짜드라마 무관(외형 표시).
