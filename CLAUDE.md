# 배구명가 — 게임 설계 문서

> 이 문서는 Claude Code가 개발을 진행하기 위한 단일 기준 문서다.
> 모호하면 이 문서의 "설계 원칙"을 우선한다. 새 결정은 이 문서에 반영한 뒤 진행한다.

---

## 1. 한 줄 요약

모바일 배구 단장/감독 시뮬레이션. 가상 V리그(여자부)를 무대로, 플레이어는 구단을
장기간(수십 시즌) 운영한다. 평소 경기는 AI가 자동으로 굴리고, 중요한 경기만
직접 개입한다. 모티브는 "백년야구".

---

## 2. 핵심 기둥 (우선순위 = 설계 충돌 시 판단 기준)

1. **관전형 장기 시뮬 (2026-06-11 정정, 구 "방치형")** — 조작하는 게임이 아니라 **보는 게임**이다.
   자동진행이 기본이고, 핵심 경험은 내 팀 경기와 시즌 서사를 **관전**하는 것.
   따라서 경기 보드의 연출 품질(실제 배구처럼 보이기)은 부차 요소가 아니라 1순위 투자처다.
   자동진행·무푸시·"매 순간 손이 가지 않게" 원칙은 그대로 유지(6장 정의).
2. **데이터 누적 서사** — 가상 선수의 기록이 세월 쌓이며 의미(스토리)가 된다.
3. ~~**현실적 밸런스 (안티 과금)** — 선수는 노쇠하고 망한다. 과금이 현실성을 깨면 안 된다.~~
   → **정정(2026-06-30, 사용자 결정)**: 유료 소비재 **다이아**(전지훈련으로 선수 강화) 도입으로 안티과금 기둥을
   명시적으로 뒤집음 — 과금이 전력에 영향(P2W)을 준다. 선수 노쇠·하락 자체는 유지. 정본 `docs/MONETIZATION_SYSTEM.md` §11.
4. **단장 의사결정의 무게** — 드래프트/FA/외국인 영입이 전력을 좌우한다. (트레이드는 제외, 7장)

> 충돌 예시: "감독으로 매 랠리 개입" vs "관전형". → 1번이 우선이므로
> **선택적 개입**으로 해결한다 (아래 4장). 보는 건 권장, 조작 강요는 금지.

---

## 3. 플레이어 역할 — 구단주의 건의 (2026-06-11 정정)

플레이어는 **구단주**다. 현장 권한(선발·교체·타임아웃)은 항상 **감독**에게 있고,
구단주는 **건의**한다 — 감독은 성향(카리스마·합리)에 따라 수락하거나 거절한다(OWNER_SYSTEM
벤치 건의와 동일 패턴). "감독으로 핸들을 잡는" 모드는 없다.

### 구단주 권한표 (어디까지 내 마음대로인가)

| 영역 | 권한 | 비고 |
|---|---|---|
| **직접 결정 (프런트 권한)** | | 현실에서도 단장·구단 소관 |
| 선수 계약 — 재계약·방출·FA 영입·드래프트 지명 | ✅ 직접 | 단, 방출 하한(10명)·캡·운영 자금이 막을 수 있음 |
| 외국인 — 트라이아웃 지명·재계약·시즌 중 교체 | ✅ 직접 | 교체는 시즌 1회 |
| 스태프 — 감독 선임(교체)·코치·스카우터 계약 | ✅ 직접 | 감독이 말 안 들으면 바꾸는 게 구단주의 칼 |
| 재정 — 운영 자금 사용 | ✅ 직접 | 모기업·관중·굿즈 수입 안에서 |
| 훈련 방향 설정 | ✅ 직접(구단 방침) | 감독 기본값 오버라이드 |
| **상대 의지에 막히는 것** | | 결정해도 거부당할 수 있다 |
| 선수 면담(설득) | ⚠ 선수가 거절·결렬 가능 | 들볶으면 안 만나줌 |
| 재계약 제안 | ⚠ 불만 선수는 뿌리침 | 면담이 좌우 |
| **건의만 (현장 권한 = 감독)** | | 감독 성향따라 수락/거절 |
| 선발 기용·벤치 지시 | 🗣 건의 | 에이스 빼라면 강하게 저항 |
| ~~세트 중 작전 방침(핀치서버 등 토글)~~ | 🚫 제거(2026-06-18) | 감독이 자동 집행(엔진 DEFAULT_POLICY). 구단주 수동 토글은 관전형과 어긋나 제거 — 타임아웃 제거와 같은 결 |
| **불가 (월권 금지)** | | |
| 타임아웃·교체 등 경기 중 현장 운영 | 🚫 | 감독 고유 권한 — 건의도 안 한다(2026-06-16 정정). 흐름 보고 부르는 건 감독 |
| 랠리 단위 지시(토스 타겟 등)·감독 무시 직접 라인업 | 🚫 | 넣지 않는다 |

### 경기 관련 건의 (랠리 위 레이어만)
- 경기 전: 선발 기용 건의("이 선수를 선발로/빼고") — 감독 판정
- ~~세트 중: 작전 방침 토글(핀치서버/블로킹/수비)~~ — 제거(2026-06-18). 감독이 자동 집행(엔진
  DEFAULT_POLICY 유지), 구단주 수동 토글은 관전형 강화 차원에서 보드에서 뺐다(타임아웃 제거와 같은 결)
- 빅매치: 관전만. **타임아웃은 감독 고유 권한**이라 건의하지 않는다(2026-06-16 정정 —
  구단주가 코트 옆에서 타임아웃을 부르는 건 현실에 없고 권한 분리를 흐림). 흐름이 밀리면
  감독이 알아서 부른다(MATCH_SYSTEM 7.4). 마음에 안 들면 → 감독 교체.

> **금지**: 매 토스마다 "누구에게 줄지" 수동 선택 + 감독을 건너뛰는 직접 지휘.
> 개입은 항상 "건의/방침" 레벨이고, 판단은 감독이, 실행은 엔진이 한다.
> 감독이 마음에 안 들면 — 그게 바로 감독 교체(STAFF)의 이유가 된다.

---

## 4. 경기 시뮬레이션 엔진 (게임의 심장)

### 4.1 랠리 체인 (1점이 나기까지의 판정 흐름)

```
서브 ──> 리시브 ──> 세트(토스) ──> 공격 ──┬─> 득점 (공격 성공)
                                          ├─> 블로킹 차단 (상대 득점)
                                          └─> 디그 성공 ──> [상대 공격으로 전환, 루프]
```

- 야구와 달리 **랠리는 루프다.** 디그가 성공하면 공수가 바뀌어 체인이 다시 돈다.
  한 점에 체인이 2~3바퀴 돌 수 있다. 랠리 길이 자체가 변수.
- 각 단계는 능력치 + 확률 판정. 시드 기반 결정론적 시뮬을 쓴다(같은 시드 = 같은 결과,
  리플레이·검증 가능).

### 4.2 단계별 판정 개요 (의사 공식, 튜닝 대상)

```
서브 에이스 확률   = f(server.serve, receiver.recv)
서브 범실 확률     = 고정 기저 + 서브 공격성에 비례
공격 성공 점수     = hitter.atk × (0.6 + 0.4 × setter.set/100)   // 세터가 승수
블로킹 차단 확률   = f(blocker.blk, hitter.atk)
디그 성공 확률     = f(digger.dig, 공격 강도)
```

> 위 계수는 전부 placeholder다. 밸런싱 단계에서 시즌 통계가 현실 V리그
> 분포(세트 스코어, 공격 성공률 ~40~55% 등)에 수렴하도록 튜닝한다.

### 4.3 로테이션 (야구에 없는 핵심 변수)

- 서브권을 따낼 때(사이드아웃) 6명이 시계방향 1칸 회전.
- 전위(2·3·4번)만 네트 앞 공격/블로킹 가능. 후위는 후위 공격만.
- **세터가 전위냐 후위냐**에 따라 공격 옵션이 바뀐다.
- 후위로 간 미들블로커는 리베로와 교체(수비 전문). 규칙 반영 필수.
- 로테이션을 빼면 "배구"가 아니라 "숫자 야구"가 된다. 반드시 구현.

### 4.4 세트/경기 규칙

- 1~4세트 25점, 5세트 15점. 모두 듀스(2점차) 적용.
- 5세트 3선승. 한 팀이 3세트 먼저 따면 경기 종료.
- 랠리포인트제: 모든 랠리가 점수로 직결.
- 매 세트 서브권 시작 팀 교대.

---

## 5. 선수 스탯 — 2층 구조

화면에 보이는 종합 스탯은 밑단 세부 스탯의 조합으로 산출된다.
"위치선정·반응속도"는 개별 기술이 아니라 **여러 기술 밑에 깔리는 공통 속성**이다.

### 5.1 밑단 (세부 스탯, 엔진 계산용)

| 분류 | 스탯 | 성장 특성 |
|---|---|---|
| 신체 | 키 | 고정 (나이 무관) |
| 신체 | 점프력 | 노쇠 시 하락 |
| 신체 | 민첩성 | 노쇠 시 하락 |
| 신체 | 체력(stamina max) | 노쇠 시 하락 |
| 신체 | 체젠(stamina regen) | 노쇠 시 하락 |
| 공통 | 반응속도 | 경험으로 성장(노쇠 없음) |
| 공통 | 위치선정 | 경험으로 성장(노쇠 없음) |
| 멘탈 | 집중력(clutch) | 경험으로 성장 |
| 멘탈 | 기복(consistency) | 경험으로 성장 |
| 멘탈 | VQ(배구 IQ) | 경험으로 성장 |

> **체력 vs 체젠**: 체력은 보유 상한, 체젠은 회복 속도. 둘을 나눠 "후반에 무너지는
> 선수 / 금방 회복해 체력전에 강한 선수"를 구분(MATCH_SYSTEM 7.1).
> **VQ**: 포지션 폴트(평균 VQ)·공격 코스·블로킹 타이밍 판단에 관여(MATCH_SYSTEM 0장).
> **기세(momentum)는 선수 스탯이 아니라 팀 단위 임시 수치**다(MATCH_SYSTEM 7.2).
> **정정(2026-06-30, 엔진 대조)**: 반응속도 ~~"완만한 곡선"~~ · 위치선정 ~~"경험으로 성장(느린 하락)"~~ →
> 둘 다 **노쇠 없음**(경험으로 느리게 ↑만). `engine/aging.ts DECAY_STATS`는 신체 4종(점프·민첩·체력·체젠)뿐이고
> 반응·위치선정은 비노쇠 그룹 — TRAINING_SYSTEM §1.6·가드 `_dv_drift2_agility`(반응·위치 Δ0 대조)가 정본. CLAUDE 표현만 뒤늦게 일치시킴.

### 5.2 윗단 (종합 스탯, 표시 + 엔진 입력)

```
스파이크 = f(키, 점프력, 공격기술) × consistency
블로킹   = f(키, 점프력, 반응속도, 블로킹기술)
디그     = f(민첩성, 반응속도, 위치선정, 디그기술)
리시브   = f(반응속도, 위치선정, 리시브기술)
세팅     = f(세팅기술, 집중력)               // 팀 공격 전체에 곱해지는 승수
서브     = f(서브기술, 집중력)
```

> "공격기술/블로킹기술/디그기술/리시브기술/세팅기술/서브기술"은 훈련·경기로
> 성장하는 순수 기술치. 밑단 신체/공통 스탯과 곱해져 윗단이 나온다.

### 5.3 포지션별 가중치 (스카우팅·드래프트 차별화의 엔진)

색이 진할수록 핵심. (3=핵심, 2=보통, 1=낮음, ·=무관)

| 스탯 | 세터 S | 아웃사이드 OH | 아포짓 OP | 미들 MB | 리베로 L |
|---|---|---|---|---|---|
| 키 | 2 | 2 | 2 | 3 | · |
| 점프력 | 1 | 3 | 3 | 3 | 1 |
| 체력 | 2 | 3 | 2 | 2 | 3 |
| 체젠 | 2 | 3 | 2 | 2 | 3 |
| 서브 | 2 | 3 | 3 | 2 | · |
| 리시브 | 1 | 3 | · | 1 | 3 |
| 세팅 | 3 | 1 | · | 1 | 1 |
| 스파이크 | · | 3 | 3 | 3 | · |
| 블로킹 | 1 | 2 | 2 | 3 | · |
| 디그 | 2 | 2 | 1 | 1 | 3 |
| VQ | 3 | 2 | 2 | 2 | 2 |
| 멘탈 | 3 | 2 | 2 | 2 | 2 |

핵심 함의:
- **리베로**: 키 무관. 키 작아도 디그·리시브만 최상이면 됨. 공격/블로킹/서브 불가.
- **세터**: 본인은 공격 거의 안 함. 세팅이 팀 공격수 전체에 곱해지는 승수. 영입 가치 큼.
- **미들블로커**: 신체 의존도 최고 → 전성기 짧고 노쇠 빠름. "전성기 짧은 선수" 서사.
- **아포짓(OP)**: 여자부에서 외국인 선수 자리. 팀 공격의 핵. 용병 결정이 가장 극적.

---

## 6. 장기 시스템 (백년야구식 누적 서사)

- **훈련·성장**: 현실처럼 아주 천천히 성장(스탯별 숨은 XP 바 + 스탯별 포텐셜).
  훈련은 12종으로 세분, **감독 선호(핵심2+보조3)** 에 따라 팀마다 다른 스탯이 성장 →
  장기 분화. 상세는 `docs/TRAINING_SYSTEM.md`.
- **나이/성장/노쇠 곡선**: 신체 스탯은 전성기 후 하락. 기술은 더 오래 유지.
- **명예의 전당 / 영구결번 / 누적 기록**: 가상 선수 통산 기록 영구 보존.
- **시즌 아카이브**: 우승/MVP/개인 타이틀 기록을 연표로.
- **환생/세대교체**: 은퇴 후 유망주 유입으로 100년+ 운영 지속.
- **시상식(2026-06 구현)**: MVP·챔프MVP·신인상·기량발전상·기록왕·베스트7·라운드MVP.
  상은 OVR이 아니라 실제 코트 생산에 준다. → `docs/AWARDS_SYSTEM.md`
- **기록 경신 마일스톤(구현)**: 개인 통산·구단·레전드 추월을 "사건"으로. → `docs/MILESTONE_SYSTEM.md`
- **플레이어 업적(2026-06 구현)**: 구단주 본인의 장기 성취(우승·시상·레전드·기록·운영)를 트로피로.
  누적 데이터 파생·저장 없이 재계산(관전형 척추 — 자발적 목표). → `docs/ACHIEVEMENT_SYSTEM.md`
- **선수 특성(구현)**: 클러치·대기만성·유리몸 등 긍정+부정. id 시드 결정론, 엔진은
  `player.traits`만 읽음(소폭 보정). → `docs/TRAIT_SYSTEM.md`
- **부상(구현)**: 엔진 밖 시즌 계층에서 "그날 출전 명단"을 깎음(결정론 보존). 경미 위주,
  동시부상 상한 3. → `docs/INJURY_SYSTEM.md`
- **뉴스 피드(구현)**: 위 산출물을 읽을 수 있는 연대기로(캡스톤). → `docs/NEWS_SYSTEM.md`
- **중계 현수막(2026-06-17 설계 · Phase1·3 구현)**: 우승·기록 경신·PO 확정 + **경기 중 실시간(세트획득·연속 득점·에이스/블록 누적)** 을
  경기 보드 하단 현수막으로(관전형 연출). 결과-결정 사건은 관전 종료 후, 결과-중립(기록)·실시간 사건은 관전 중 — 스포일러 정책. → `docs/BROADCAST_SYSTEM.md`
- **구단 정체성(2026-06-18 구현)**: 게임 시작부터 구단마다 역사·색깔(명문·신흥강호·황혼의 명가·
  만년약체·신생팀 등 고정 배정). 선택 화면에 서사가 뜨고 **선수단 구성이 실제로 맞물림**(강·약·나이/포텐).
  시드 생성+표시만, 엔진 무파급 — 정체성은 "시작 조건"이라 수 시즌 뒤 평균회귀. → `docs/CLUB_IDENTITY_SYSTEM.md`
- 데이터 정확도보다 "세월이 쌓여 만들어지는 이야기"가 목표 (백년야구 철학).

> **관전형의 정의(2026-06 확정, 06-11 표현 정정)**: 시즌은 앱 사용 중에만 진행되며 **푸시 알림은 없다.**
> 모든 서사 전달은 앱 안에서 볼 때 조용히 surface(피드·위젯·연표). 강제 관람 없음 —
> 단, 핵심 경험은 "보는 것"이므로 보고 싶어지게 만드는 것(경기 연출·빅매치 표시)이 제품의 일이다.

---

## 7. 단장 의사결정 루프

| 시스템 | 내용 | 여자부 특이점 |
|---|---|---|
| 신인 드래프트 | 유망주 영입, 스탯 일부만 공개(스카우팅) | — |
| FA | 자유계약 선수 영입/유출 | — |
| ~~트레이드~~ | ~~선수 맞교환~~ | **제외(2026-06 결정).** 방치형과 결이 약하고 AI 거래 밸런스 난해. 선수 수급은 드래프트/FA/용병으로 |
| 외국인 선수 | 아포짓 위주 1~2명. 팀 전력 좌우 | **비중 최대.** 핵심 레버 |
| 감독 선임/해촉 | 감독 스탯(카리스마)·성향(공격/수비/밸런스) 선택 | 자동 경기 스타일·타임아웃 결정 |
| 스태프 계약 | 전문 코치(포텐/노쇠/멘탈)·스카우터(드래프트 공개도) 영입(예산 내) | `docs/STAFF_SYSTEM.md` |
| 시즌 중 이동(2026-06 구현) | 방출→FA 풀, 포지션 구멍을 FA로 긴급 수혈(전 구단 AI, 캡·정원 적용) | 부상과 직결. `docs/TRANSACTION_SYSTEM.md` |
| 구단주 레이어(2026-06-11 설계) | 선수 면담(설득 성공/역효과, 선수가 면담 거절 가능)·감독에게 벤치 건의(성향별 수락/거절)·팬심(스타 벤치→기사→관중→예산) | `docs/OWNER_SYSTEM.md` |
| 경기감각(2026-06-11 설계) | 결장 누적 시 체감 −7%까지(주전은 무변화). 출전 이력 파생, 부상 복귀·벤치 복귀 비용 | `docs/FORM_SYSTEM.md` |

> FA/드래프트/세대교체 상세(등급·보상선수·보호명단·프랜차이즈·샐러리캡)는 `docs/FA_SYSTEM.md`.
> 연봉·계약은 `docs/SALARY_SYSTEM.md`. 경기 엔진은 `docs/MATCH_SYSTEM.md`.
> 시상식·마일스톤·특성·부상·뉴스·시즌 중 이동은 각 `docs/*_SYSTEM.md`(6장 참조).
> 시스템별 구현 현황·문서 색인은 `docs/README.md`.

---

## 8. 기술 스택 & 아키텍처

- **프레임워크**: React Native + Expo (Expo Go 실행)
- **언어**: TypeScript
- **상태관리**: Zustand (가벼운 단일 스토어. 시뮬 상태가 크므로 selector 활용)
- **온라인 전환(2026-06-30, 사용자 결정 — "오프라인 기둥" 폐기)**:
  - ~~**MVP는 로컬 우선.** 오프라인 싱글 방치형이라 서버 불필요.~~ → **정정**: 유료 재화(다이아)·결제·계정이
    들어오면서 **온라인 기반으로 전환**한다. **소셜 로그인(구글/애플)** 으로 계정을 만들고, **Vercel 서버**가
    다이아 지갑·로그의 **진실의 원천**이며 ~~**스토어 영수증 검증을 직접**(구글 Play/애플 API, RevenueCat 미사용) 수행한다~~
    → **정정(2026-07-03, 사용자 결정)**: **RevenueCat 재채택** — RC를 영수증 검증·환불 웹훅·엔타이틀먼트(광고제거·DLC) 게이트웨이로 쓰되,
    **다이아 잔액 진실은 계속 우리 원장**(RC virtual currency 금지). 정본 `BACKEND_SYSTEM.md` §13.18. 관전형(보는 게임)·누적 서사 기둥은 유지하되 **"오프라인에서도 돈다"는 기둥은 내려놓는다.**
    설계 정본은 `docs/BACKEND_SYSTEM.md`(+수익화는 `docs/MONETIZATION_SYSTEM.md`).
  - **게임플레이(시즌 시뮬)는 여전히 로컬 결정론** — base 스냅샷+currentDay+results 시드 리플레이는 그대로다.
    서버는 **재화·계정·결제·로그·문의·통계**만 맡고 **시드/리플레이엔 절대 안 들어간다**(결정론 격리). 즉 온라인은
    "재화·신원·결제"를 위해 필요하고, 경기/순위 재생은 로컬이 한다 — **관전·시즌 시뮬은 연결이 끊겨도 캐시로 계속 동작**한다.
    **단 다이아 사용(전지훈련)·적립(광고/업적)·결제는 무조건 온라인**: 잔액 *표시*는 캐시를 쓰되 **쓰거나 받는 건 서버 확인
    후에만 반영**(전지훈련 차감도 서버 차감 성공 뒤 campLog 기록 → 서버↔로컬 split-brain 자체가 안 생김). 정본 `docs/BACKEND_SYSTEM.md`.
  - **현 구현: zustand persist + AsyncStorage 단독**(세이브는 리플레이 기반이라 작음 —
    base 스냅샷 + currentDay + results). `expo-sqlite`는 보류(필요해지면 도입, `db/schema.ts`는 TODO).
  - **전환 완료(2026-06-27, B안 — Phase 0~3 ✅)**: ~~게으른 씨앗 재생(켤 때마다 순위·생산 재시뮬)~~ → **계산 결과를
    세이브에 저장하고 재로드 시 읽기**(로딩·재생버그 제거). 재생 엔진은 폴백으로 유지(구세이브·새시즌·과거관전·상태변경).
    엔진버전 게이트로 재튜닝 후 일관성. 단계·함정 7게이트·전환 요약은 `docs/REALTIME_SIM_SYSTEM.md`.
    독립 리뷰는 C(캐시) 권고했으나 사용자가 B 채택(결과적으로 캐시-영속으로 수렴 — 그 문서에 보존).
  - **세이브 마이그레이션(2026-06-26)**: `version`+`migrate`(정규화기)+안전 복원으로 **출시 후 구조 변경에도
    사용자 세이브가 안 깨진다**(필드 추가=자동, 모양 변경=버전+변환). 영속 52필드 스키마·정책은 `docs/SAVE_SYSTEM.md`.
  - ~~Supabase는 **나중에** 클라우드 백업·글로벌 명예의전당·랭킹 붙일 때 옵션으로. MVP에서 서버부터 깔면 방치형 본질과 어긋나고 개발만 무거워짐.~~
    → **정정(2026-07-02, 사용자 결정)**: 온라인 전환(위)으로 백엔드를 지금 구축 중이라, **Supabase를 Vercel 서버의 Postgres 호스트로 채택**(dev·prod 공통, 로컬 Docker 폐기).
    Supabase는 **DB 호스트로만** 쓴다(Auth·Realtime·Storage 미사용 — 인증은 자체 Bearer). 결정론 격리는 유지: 서버 DB는 재화·계정·결제·로그·문의·통계만, 시드/리플레이엔 안 들어감. 연결 규칙(풀러:6543 `prepare:false` / 마이그레이션:5432)은 `docs/BACKEND_SYSTEM.md` §13.7.
- **네비게이션**: expo-router (파일 기반)
- **시뮬 엔진**: 순수 TS 모듈(`/engine`). React 의존성 0. 시드 결정론. 단독 테스트 가능.
  - 엔진은 "상태 + 시드 → 다음 상태"의 순수 함수 집합으로. UI와 완전 분리.

### 디렉터리(제안)

```
/app                 expo-router 화면
  /(tabs)
    index.tsx        대시보드(구단 현황)
    squad.tsx        선수단/스탯
    schedule.tsx     일정/경기
    office.tsx       단장 업무(드래프트/FA/용병)
    history.tsx      기록/명예의전당
  match/[id].tsx     경기 보드(관전/개입)
/engine              순수 TS 시뮬 (React 무관)
  rng.ts             시드 기반 RNG (mulberry32 등)
  rally.ts           랠리 체인 판정
  rotation.ts        로테이션 규칙
  match.ts           세트/경기 진행
  season.ts          시즌 진행·일정
  aging.ts           성장·노쇠 곡선
/data                초기 가상 리그/선수 시드 데이터(JSON)
/store               zustand 스토어
/db                  sqlite 스키마·마이그레이션·쿼리
/types               공용 타입
```

---

## 9. 데이터 모델 (초안)

```ts
type Position = 'S' | 'OH' | 'OP' | 'MB' | 'L';

interface Player {
  id: string;
  name: string;
  age: number;
  position: Position;
  isForeign: boolean;
  // 밑단
  height: number;        // 고정
  jump: number; agility: number; stamina: number;        // 신체
  reaction: number; positioning: number;                 // 공통
  focus: number; consistency: number;                    // 멘탈
  // 기술치(성장)
  skSpike: number; skBlock: number; skDig: number;
  skReceive: number; skSet: number; skServe: number;
  // 메타
  peakAge: number;       // 전성기 나이(노쇠 곡선용)
  career: CareerStats;   // 통산 누적
}

interface Team {
  id: string; name: string;
  players: string[];      // player ids
  coachStyle: 'attack' | 'defense' | 'balanced';
  foreignSlots: number;   // 외국인 보유 한도
}

interface MatchState {
  seed: number;
  setNo: number;          // 1..5
  points: { home: number; away: number };
  sets: { home: number; away: number };
  serving: 'home' | 'away';
  rotation: { home: number; away: number };  // 0..5
  over: boolean;
}
```

---

## 10. 개발 로드맵 (Phase)

### Phase 0 — 엔진 코어 (UI 없이 콘솔/테스트) ✅
- [x] 시드 RNG
- [x] 랠리 체인 판정 (서브→득점, 디그 루프 포함)
- [x] 로테이션 규칙
- [x] 세트/경기 진행(25·15점, 듀스, 3선승)
- [x] 한 경기 시뮬을 콘솔에서 끝까지 돌려 통계 출력 (`tools/sim*.ts`)
- [x] 밸런싱: KOVO 분포 수렴(킬 ~56%·스터프 ~10%·에이스 ~6%)

### Phase 1 — 경기 보드 UI (관전) ✅
- [x] 코트 + 마커 + 점수판
- [x] 랠리 진행/자동/리셋
- [x] 경기 로그
- [x] 엔진과 UI 연결(엔진 상태를 보드가 렌더)

### Phase 2 — 선수단 & 스탯 ✅
- [x] 가상 리그 시드 데이터 생성기
- [x] 선수 카드/스탯 화면 (2층 구조 표시 + 특성 뱃지)
- [x] 포지션별 가중치 반영된 종합 스탯 산출

### Phase 3 — 시즌 루프 (방치형 본체) ✅
- [x] 일정 생성, 시즌 자동 진행
- [x] AI 감독 자동 라인업/작전
- [x] 시즌 결과·순위·통계 누적
- [x] 로컬 저장 + 이어하기 (sqlite 대신 zustand persist + AsyncStorage로 구현)

### Phase 4 — 구단주의 경기 개입 = 건의 (2026-06-11 재정의) ✅
- [x] 중요 경기(빅매치) 표시 + 관전 유도 (순위 직결 판정 — isBigMatch)
- [x] 선발 기용 건의 (감독 수락/거절 — 벤치 건의 패턴, 선수 상세)
- ~~세트 중 작전 방침 토글 (SubPolicy)~~ — 제거(2026-06-18). 감독이 자동 집행(엔진 DEFAULT_POLICY 유지), 구단주 수동 토글은 보드에서 뺐다(관전형 강화 — 타임아웃 제거와 같은 결)
- ~~관전 중 타임아웃 건의~~ — 제거(2026-06-16). 타임아웃은 감독 고유 권한이라 건의 자체를 뺐다(3장). 감독이 자동 호출(MATCH_SYSTEM 7.4)

### Phase 5 — 단장 업무 ✅
- [x] 드래프트(스카우팅 일부 공개 — 스카우터 역량 연동)
- [x] FA 영입 (트레이드는 제외 — 7장. 시즌 중 이동은 TRANSACTION_SYSTEM)
- [x] 감독 선임 + 스태프 계약(전문 코치·스카우터)

### Phase 6 — 장기 서사 ✅
- [x] 성장·노쇠 곡선
- [x] 통산 기록 / 명예의전당 / 영구결번
- [x] 시즌 아카이브 연표 + 시상식·마일스톤·뉴스 피드

### (이후) Phase 7 — 클라우드(옵션)
- [ ] Supabase 백업/복원
- [ ] 글로벌 명예의전당·랭킹

---

## 11. 작업 원칙 (Claude Code 준수)

- 엔진(`/engine`)은 React/Expo에 의존하지 않는다. 순수 함수 + 시드. 테스트 먼저.
- UI는 엔진 상태를 "그리기만" 한다. 게임 로직을 UI에 두지 않는다.
- 관전형이 1순위. 어떤 기능이 "매 순간 손이 가게" 만들면 설계를 의심한다.
  반대로 "보는 경험"(경기 연출·중계 가독성)의 품질은 핵심 투자처다.
- 과금/부스트가 현실성·밸런스를 깨는 방향이면 넣지 않는다.
- Expo Go에서 항상 실행 가능한 상태를 유지한다(네이티브 모듈 추가 시 사전 확인).
- 새 설계 결정은 코드보다 먼저 이 문서에 반영한다.
- **추정 절대 금지 (최우선)**: 엔진의 수치·분포·동작·구조·"수정 효과"를 추론으로 단정하지 않는다.
  통계가 없으면 시뮬을 돌려 확인한다(N≥10,000). 코드만 읽고 "당연히 그럴 것"도 금지 — 돌려서 본다.
  새로 만든 측정 도구도 검증 없이 믿지 않는다(허위 오라클 금지 — sanity-check + 수정 전후 A/B).
  검증 안 된 수정은 커밋하지 않고 되돌린다. 상세·실패 사례는 `docs/STATS_PROTOCOL.md` 0장.
- **통계 프로토콜**(`docs/STATS_PROTOCOL.md`): 표본 1만+, 문서 기입 시 `(N · 엔진 커밋 · 날짜)` 필수,
  엔진 로직 변경 시 기존 통계는 재측정하거나 "재시뮬 필요"로 무효 처리.

### 표준 작업 순서 (모든 작업 — 2026-06-26 명문화)

어떤 작업(기능·수정·연출·엔진 변경)이든 아래 4단계를 순서대로 밟는다. 흩어져 있던
"문서 먼저 → 개발 → 검증"(DOC_DISCIPLINE §1·STATS_PROTOCOL·EDGE_CASES §4)을 하나로 묶은 것.

1. **플랜** — 착수 전, 그 영역의 `docs/*_SYSTEM.md`(+`BOARD_RULES`/`UI_RULES`)에서 **이미 내려진·상충
   결정을 먼저 읽고**("내 변경이 그걸 뒤집나?"), 목표를 단계로 분해한다. "작은 표시/UI 한 줄"도
   문서화된 동작(컷오프·스포일러·귀속·밸런스)을 바꾸면 시스템 변경 — 먼저 검색.
1.5. **독립 리뷰 (플랜/설계가 나오면 — 2026-06-26 룰)** — 새 시스템·설계 갈림길·기획 결정이 선
   **`Agent`(새 세션·general-purpose)를 띄워 "이 설계가 좋은지, 더 나은 안이 있는지" 편향 없이 검토**받고
   진행한다. 게임 두 기둥(관전형·누적서사)·제약(무저장 결정론·가짜드라마 금지·1만시즌+)을 프롬프트에 줘서
   원칙 위반·용어 모순·더 나은 대안을 잡는다(예: HOF 등번호 리뷰 — "계승자"가 가짜드라마라 "번호 계보"로 교정).
   리뷰 결과를 문서(2단계)에 반영. **사소한 기계적 수정은 제외**(설계 판단이 끼는 플랜만).
2. **문서 작성** — 설계·단계 계획을 **코드보다 먼저** 해당 `docs/*_SYSTEM.md`(또는 `CLAUDE.md`)에
   반영하고 `docs/README.md` 색인을 갱신한다. 기존 결정을 뒤집으면 취소선 정정으로 보존(DOC_DISCIPLINE).
3. **개발** — 문서에 적힌 계획·통과 조건·측정 도구를 코드가 구현한다. 다단계면 **각 단계 착수 전**
   그 단계 계획이 문서에 있는지 확인(없으면 먼저 적고 시작).
4. **오류 확인** — 추정 금지: 수치·동작·수정 효과를 시뮬로 검증(N≥10,000, A/B 자가검증으로 도구
   민감도 증명). 풀 배터리 0건이어야 커밋. 버그를 찾으면 5단계 프로토콜(`TEST_METHODOLOGY §2`:
   재현·사각분석·케이스등록·형제사냥·방법기록). 검증 안 된 수정은 커밋하지 않고 되돌린다.

> 완료하면 그 단계를 문서에 "✅ 완료(날짜)"로 표시하고 새 측정/감사 도구를 `docs/README.md`
> 검증 루틴에 등록한다(취소선 정정 보존). 상세는 `reload-docs` 스킬 §"모든 작업은 문서부터".

### SOLID / 시스템 분리 (수정 파급 최소화)
- **단일 책임**: 각 시스템(훈련/노쇠/경기/생산/연봉…)은 `engine/`의 독립 순수 모듈.
  훈련을 바꿔도 경기·생산이 영향받지 않게 한다.
- **의존 방향**: UI(`app/`) → 셀렉터(`data/`) → 엔진(`engine/`). 역방향 금지.
- **계약(타입)에만 의존**: 엔진끼리는 구현이 아니라 출력 타입에만 의존한다.
  예) `production`은 경기 구현을 모른 채 `SimResult` 형태에만 의존 → 경기 엔진 교체 가능.
- **오케스트레이션은 함수 호출 합성**: "진행"은 `advance`(무엇을 할지) + 진화 셀렉터(훈련/노쇠)
  + `simMatch`(경기) + `production`(집계)을 **호출로 조합**한다. 시스템은 갈아끼울 수 있어야 한다.
- **결정론 유지**: 모든 시스템은 시드 기반 순수 함수. 상태는 저장 대신 `currentDay`+`results`에서
  재계산(리플레이)한다.
